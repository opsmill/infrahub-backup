Kubernetes deployments whose Neo4j Enterprise or PostgreSQL task-manager database runs outside the cluster can now be backed up and restored with the same `infrahub-backup create` and `restore` commands as any other deployment. Artifacts taken from an external database are byte-compatible with internal ones and restore by the same path, and deployments whose databases are all internal behave exactly as before.
